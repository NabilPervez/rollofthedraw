Plan: Make layout responsive
1. Use flex-wrap or grid for main flex layouts (especially header and main area) to ensure they stack properly on mobile.
2. Ensure flex-direction is column on mobile for the main container (`flex: 1` split).
3. Use media queries to change font sizes and padding on small screens.
